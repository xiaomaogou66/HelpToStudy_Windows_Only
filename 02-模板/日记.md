---
type: daily
date: {{date}}
---

# 📆 {{date}}

> 两个查询都是**实时镜像**：点复选框会写回**源文件**，所以不会出现"两处打卡不一致"。
> 源文件：打卡项在 `06-日志/<周号>/打卡.md`（按课表生成）；学习块在 `06-日志/<周号>/<周号>.md`（周决策，也是日历的源）。
> 本篇住在 `06-日志/<周号>/`（一周一个文件夹：周决策 + 打卡页 + 日记，见 [[05-日程安排/06-日志/说明-怎么打卡|怎么打卡]]）。
> ⚠️ 下面两个查询都按**本页 frontmatter 的 `date`** 查（`this.date`），所以**7 篇日记各显示各的那天**，
> 不会都显示"今天"。（首页里的「📅 今天」才是按 `date(today)` 查的。）

## ✅ 今日底线（打卡）

> 条目跟着课表走：每节课一项；只有指定的那两门课挂「课上 Anki」；周六挂「地铁泛听」。
> 规则见 `05-日程安排/00-配置/底线规则.json`。

```dataview
TASK
FROM "05-日程安排/06-日志"
WHERE contains(text, dateformat(date(this.date), "MM-dd") + " · ")
SORT completed ASC, text ASC
```

## 📅 今天要做什么

```dataview
TASK
FROM "05-日程安排/06-日志"
WHERE due = date(this.date) OR scheduled = date(this.date)
SORT completed ASC, due ASC
```

## 🤔 卡住的地方 / 下次要问的问题

-

## 📊 一句话复盘

-

## 🎯 明天第一个动作

-
