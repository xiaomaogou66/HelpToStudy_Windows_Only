# 更新记录

## v1.1.0（2026-09-23）

> 版本对应：本次内容与 Linux 版 **v0.3.0** 对齐（本仓库沿用 v1.0.x 版本线，故记为 v1.1.0）。

**与 Linux 版对齐：新增第六层「日程安排」**（周决策 / 打卡 / 课表 / 日历双后端），
并把**拆书策略统一为 MinerU 单流程**（本地文字层提取下线）。

### 拆书：策略统一（**破坏性变更，请先看这条**）

- **全走 MinerU**：PDF → MinerU 云端 OCR（公式转 LaTeX）→ 按章拆分。
  本地提取（`pdfplumber` / Word `.docx` / EPUB）**已下线**：
  - `_工具\拆书.bat` 成为**唯一入口**（原 `拆书-MinerU.bat` 保留为转接壳，旧快捷方式仍可用）；
  - `_工具\拆书-OCR.bat`（本地 OCR）已删除；
  - EPUB / Word 请先转成 PDF 再拆。
- **启动器支持额外参数透传**，例如 `_工具\拆书.bat "D:\书\某教材.pdf" --mineru-chunk-mb 8`
- **大扫描件更稳**：上传按「≤200 页 且 ≤25 MB」分块；单份上传超时自动对半切小重传；
  Token / 额度类错误立即判定不再无谓重试。
  可调 `--mineru-chunk-mb`（分块上限 MB）、`--mineru-dpi`（扫描页降采样 DPI，0 = 不压缩；
  需要本机有 Ghostscript `gs`，没有则自动退回原文件分块上传）
- **章节识别增强**：章标题被 OCR 整批打掉时，默认改走「每章固定收尾小节」定位
  （如「作业 (Trabajos de casa)」当章界锚点），实测 16 课一次切准；
  正文标题只剩编号（`# 4 货币系统：…`）也能对上目录名；只在明显优于通用识别时才采用。
  可指定 `--chapter-end-pattern` / `--chapter-title-pattern` / `--chapter-start-pattern`，
  或 `--chapter-end-pattern off` 关闭
- **旧分块备份改到库外** `%USERPROFILE%\obsidian_backups\拆书\<书名>\`：
  不再在库里生成 `_备份\`（库内同名文件会让 Obsidian 关系图谱出现重复节点、短链接解析歧义）
- `/拆书` 命令新增**第 0 步令牌体检**：用「查一个不存在的任务」判断 Token 是否过期
  （`HTTP 401 + A0211`），报「可用 / 已过期 + 已签发多少天」，**不消耗额度、不改文件**；
  过期就停下来提示重跑 `设置MinerU令牌.bat`，不会白跑一遍整本书
- 书后部分关键词修正：补「术语表 / 译后记」，去掉会造成误切的「附录」

### 新层：日程安排（`05-日程安排\`）

- 一周一个文件夹：`06-日志\<周号>\` 里放 `<周号>.md`（周决策）+ `打卡.md`（打卡页）+ 7 篇日记
- `/周决策`（周日跑）：检索上周完成率、各科进度、检查点，生成下周周决策并预建周文件夹与日记
- **打卡项跟着课表走**：规则表在 `00-配置\底线规则.json`
  （`每节课` / `触发`（条件 → 打卡项）/ `每天都有` / `例外.停课`），改规则不用改代码
- **两个地位同等的同步后端**（`_脚本\同步日程.bat`）：
  - `ics`：写本机 `_资源\日程安排.ics`，零云依赖，可被云盘 / Gist / 手机订阅
  - `gcal`：推 Google 专用日历（服务账号或 OAuth），多端原生同步
    用法：`同步日历.bat [--dry-run|--check|--list-calendars]`，代理默认 `127.0.0.1:7897`
    （`set SYNC_PROXY=...` 可换端口或 `none` 直连），只对本脚本生效
  - 两者读同一份配置、同一套 UID、同样的提醒口径；`--后端 ics|gcal` 可单选
- **配置与引擎分家**：人只改 `05-日程安排\00-配置\`
  （`规划配置.json` 目标/里程碑/后端开关、`底线规则.json`、`同步配置.json`），引擎在 `_脚本\`
- **里程碑是唯一真源**：主页倒计时、首页「今天」、`today.bat` 三处同读一个 JSON
- **`_脚本\自检.bat`（烟测）**：配置、打卡页、周文件夹、today、两个后端、ics 结构、
  命令一致性、MinerU 令牌，一条命令全过（不联网、不改数据）
- 主页「📅 今天」区块：打卡 + 学习块两栏 Dataview 实时镜像，连续打卡天数自动算
- 教程：`00-使用指南\📖 使用说明.md`；多端同步与 Google 服务账号配置：
  `05-日程安排\00-配置\多端同步.md`（每个步骤都写清「在干什么 / 为什么 / 做完你会看到 / 跳过会怎样」）
- 开箱即用：`05-日程安排\_资源\日历\课表示例.ics`（换成自己的课表即可）

### 命令与模板

- 命令从 6 条增加到 **8 条**：新增 `/周决策`、`/课表`
- `/学案` 支持**对照教材**（主题主页 frontmatter 有 `book_alt` 时同时读对照教材，做英文术语对照）；
  典型例题要求「教材原题照抄 + 解题思路」，自编题必须标注
- `/新主题-拆解与计划`：学习计划固定命名为 `【主题名】-学习计划.md`，并同步修正主页旧链接
- 新增模板：`02-模板\周决策.md`、`02-模板\日记.md`、`02-模板\日程安排\`（4 个分篇模板）
- 同步 `01-提示词库`、`00-使用指南`、`AGENTS.md` / `CLAUDE.md`（含日程安排层约定）

### 其他

- `.obsidian\`：新增 **iCal**（本地 .ics 订阅）、**LaTeX Suite**、**Style Settings**、
  **Table Editor** 四个插件；补 `daily-notes.json`（日记落点 = `05-日程安排\06-日志`，
  格式 `GGGG-[W]WW/YYYY-MM-DD`）
- 新增 `.pi\skills\`：与 8 条命令一一对应的 skill 版（供 pi 等 agent 使用）
- 安装器：目录结构加入 `05-日程安排\` 与 `.pi\`，去掉废弃的 `_备份\`；
  依赖收敛为 `pypdf` + `mineru-open-api`，并安装日程同步依赖
  （`google-api-python-client` / `google-auth-oauthlib` / `PySocks` / `tzdata`）；
  装完可跑 `自检.bat` 验证
- `today` 与 `自检` 改为**跨平台单份实现**（`today.py` / `自检.py`）：
  Windows 用 `.bat` 薄壳、Linux 用 `.sh` 薄壳调用同一份逻辑，两端不再各写一套
- 仓库维护：`scripts\sync-from-vault.py`（跨平台，从本地库单向同步框架文件 + Windows
  措辞适配 + 通用化 + 上线前检查）
- 发布流水线：打包排除 `.venv` / `__pycache__` / 本地词表；新增 `smoke` 作业
  （Python 语法检查 + 离线自检 + 「不该进仓库的文件」检查，只报告不卡发布）